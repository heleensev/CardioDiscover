#init file of App package, aliases are globally available
import App.config as glob
import App.read_GWAS as reader
import App.classify_columns as classifier
import App.check_correct as checker
import App.rs_liftover as liftover